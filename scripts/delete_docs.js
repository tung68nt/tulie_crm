import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function main() { const { data, error } = await supabase.from('contract_documents').delete().eq('contract_id', '48ec5a59-7436-4a05-9e19-015e926304b1'); console.log('Deleted:', error ? error : 'Success, waiting to trigger via CRM'); } main();
